# .github/workflows/pull_request.yml
name: Pull Request Action

on:
  pull_request:
    types: ["opened", "reopened", "synchronize"]

jobs:
  frontend-pr-check:
    uses: Smokey-Olive/main-service/.github/reusable-workflows/frontend-pr-check.yml@main
    with:
      service-name: "main-service"

  server-pr-check:
    uses: Smokey-Olive/main-service/.github/reusable-workflows/server-pr-check.yml@main
    with:
      service-name: "main-service"
